export const BASE_URL = 'https://api.nomoreparties.co';

export const options = {
  baseUrl: "https://diplomat.nomoredomains.icu/api",
  headers: {
    "Content-Type": "application/json",
  }
};

export const REGEX_EMAIL = /^(?!\.)([^\.\s@()<>,;:\\"\[\]]\.?)+@[a-z]+([^_][a-z0-9\-]+)+\.[a-z]{2,6}$/;
